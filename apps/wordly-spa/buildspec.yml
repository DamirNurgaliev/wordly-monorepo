version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - nx build wordly-spa

artifacts:
  files:
    - '**/*'
  discard-paths: yes
  base-directory: 'dist/apps/wordly-spa/'
